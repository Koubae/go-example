Clean net/http
==============

* Date: 2026 - 03 (March) - 20th

Is "subjectivelly clean".
Not fully fledge Hexagonal Architecture for sure but follows some simple principles.

The main idea was actually to see how to use properly the DB Pool "http://github.com/jackc/pgx/v5/pgxpool"
in a almost-production ready scenario so the real focus is there, all why attempting to keep a "clean & Solid"
architecture by passing interfaces into repository + services.

By the way, code generated by ChatGPT but it looks quite correct.


### Requests

Start server 

```bash
make run-clean-net-http
```

```bash

curl -X POST http://localhost:8080/users -H "Content-Type: application/json" -d '{"name": "user123", "email": "user@example.com"}'
curl -X GET http://localhost:8080/users/1 
curl -X GET http://localhost:8080/users 
```



Create db, you should have a postgres running (user + pass both `admin`) and create the schema + db from [schema.sql](./sql/schema.sql)


